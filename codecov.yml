comment: off

coverage:
  precision: 0
  round: down
  status:
    patch:
      default:
        target: 90
    project:
      default:
        threshold: 1%
    changes: false

ignore:
  - "tests/"
  - "notebooks/"
  
